<?xml version="1.0" encoding="UTF-8"?>
<web-app version="6.0"
         xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd">

    <display-name>TrainTicketBooking</display-name>

    <!-- ===== DataSource & Mail ===== -->
    <resource-ref>
        <description>Train DataSource</description>
        <res-ref-name>jdbc/TrainDB</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <resource-ref>
        <res-ref-name>mail/Session</res-ref-name>
        <res-type>jakarta.mail.Session</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <!-- ===== Filters (THỨ TỰ QUAN TRỌNG): CSRF trước, rồi Auth ===== -->
    <filter>
        <filter-name>CsrfFilter</filter-name>
        <filter-class>vn.ttapp.security.CsrfFilter</filter-class>
        <!-- áp dụng exclude đúng CHỖ (bên trong <filter>) -->
        <init-param>
            <param-name>excludePrefixes</param-name>
            <param-value>/assets/,/static/,/favicon,/robots.txt</param-value>
        </init-param>
        <!-- BỎ QUA kiểm tra CSRF ở các endpoint auth -->
        <init-param>
            <param-name>excludeExact</param-name>
            <param-value>/auth/login,/auth/register,/auth/logout</param-value>
        </init-param>
        <!-- Nếu muốn vẫn kiểm tra ở /auth/login thì bỏ excludeExact và thêm:
        <init-param>
            <param-name>rotateOnAuth</param-name>
            <param-value>false</param-value>
        </init-param>
        và chỉnh servlet theo phương án B -->
    </filter>
    <filter-mapping>
        <filter-name>CsrfFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>vn.ttapp.security.AuthFilter</filter-class>
    </filter>
    <!-- Dùng một mapping /*, AuthFilter tự bỏ qua static/public,
    và check role cho /admin, /manager, /staff, v.v. -->
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- ===== Servlets ===== -->

    <!-- SeatMapServlet: bạn đã đổi sang /customer/seatmap để tránh trùng /seatmap -->
    <servlet>
        <servlet-name>SeatMapServlet</servlet-name>
        <servlet-class>vn.ttapp.controller.customer.SeatMapServlet</servlet-class>
        <load-on-startup>2</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>SeatMapServlet</servlet-name>
        <url-pattern>/customer/seatmap</url-pattern>
    </servlet-mapping>

    <!-- Login / Register / Logout -->
    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>vn.ttapp.controller.auth.LoginServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/auth/login</url-pattern>
    </servlet-mapping>

    <servlet>
        <servlet-name>RegisterServlet</servlet-name>
        <servlet-class>vn.ttapp.controller.auth.RegisterServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>RegisterServlet</servlet-name>
        <url-pattern>/auth/register</url-pattern>
    </servlet-mapping>

    <servlet>
        <servlet-name>LogoutServlet</servlet-name>
        <servlet-class>vn.ttapp.controller.auth.LogoutServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>LogoutServlet</servlet-name>
        <url-pattern>/auth/logout</url-pattern>
    </servlet-mapping>

</web-app>
